import type { Question } from "@/types/question";

export async function getOpenEducationQuestions(): Promise<Question[]> {
  throw new Error(
    "Provider Open Education preparado para futuras APIs educacionais abertas.",
  );
}
